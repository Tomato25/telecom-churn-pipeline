version: 2

sources:
  - name: raw
    database: telecom_dw
    schema: raw
    tables:
      - name: telco_customers
        description: "Raw telecom customer data loaded from IBM Telco CSV. All columns loaded as VARCHAR."